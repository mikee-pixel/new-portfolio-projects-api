{
    "name": "new-portfolio-projects-api",
    "version": "1.0.0",
    "main": "server.js",
    "scripts": {
        "start": "json-server --watch data/db.json --port $PORT --host 0.0.0.0"
    },
    "dependencies": {
        "json-server": "^0.17.0"
    }
}